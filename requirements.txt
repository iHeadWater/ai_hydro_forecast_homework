# Third-party packages from PyPI.
mlflow
modelscope
hydrodataset>=0.2.3
# Pin aqua_fetch: 1.0.1 removed aqua_fetch.utils.check_attributes, which
# hydrodataset still imports -> ImportError. 1.0.0 keeps it.
aqua_fetch==1.0.0

# Locally placed source (you drop these folders in yourself), installed
# editable so the code in these folders — and any edits — is what actually
# runs, NOT the PyPI release. Install from the repo root:
#     pip install -r requirements.txt
-e ./hydrodatasource
-e ./hydromodel
